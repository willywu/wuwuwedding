# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Welcome.index()
GET     /story						controllers.Story.index()
GET     /brideandgroom              controllers.BrideAndGroom.index()
GET     /registry                   controllers.Registry.index()
GET     /contact                    controllers.Contact.index()
GET     /guestbook                  controllers.GuestBook.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
